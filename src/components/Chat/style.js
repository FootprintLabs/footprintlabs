import {StyleSheet, Dimensions} from 'react-native';

const styles = StyleSheet.create({
  container: {
      height: Dimensions.get("window").height-264,
      backgroundColor: '#eaeaea'
  }
});

export default styles;