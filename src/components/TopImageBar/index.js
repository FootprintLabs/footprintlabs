import React, {Component} from 'react';

import ImageBar from './ImageBar';


export default class TopImageBar extends Component {
    render() {
        return (
            <ImageBar />
        );
    }
}